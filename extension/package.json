{
  "name": "@browser-automator/extension-chrome",
  "version": "0.1.0",
  "description": "Chrome extension for browser automation",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@browser-automator/dom-core": "workspace:*",
    "@browser-automator/extensions-core": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "@types/jsdom": "^27.0.0",
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.11",
    "jsdom": "^27.0.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
